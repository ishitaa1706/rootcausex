server.port=8080
spring.application.name=rootcausex-backend

# Phase 4 — Claude API
# Set ANTHROPIC_API_KEY env var before running, or paste key directly (do not commit key to git)
anthropic.api.key=${ANTHROPIC_API_KEY:}
anthropic.model=claude-sonnet-4-6
